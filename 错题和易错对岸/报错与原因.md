1 Object of type "Literal[0]" is not callable

常见于内置函数被当作变量名使用了

当你写 max(...) 时，Python 试图“调用”一个整数 → 运行时报错：TypeError: 'int' object is not callable
而 IDE 的类型检查器（如 Pylance）会提前警告：Object of type "Literal[0]" is not callable
